# Media Analyzer CLI - Environment Variables
# Copy this file to .env and fill in your API keys

# API Keys for various LLM providers

# OpenAI API Key (for GPT models and Whisper transcription)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Azure OpenAI (alternative to OpenAI - has priority for Whisper models)
AZURE_OPENAI_KEY=your-azure-openai-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# Anthropic API Key (for Claude models)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# Google/Gemini API Key (for Gemini models)
GEMINI_API_KEY=your-google-api-key-here
GEMINI_API_KEY=your-gemini-api-key-here

# Default settings (optional - can be overridden via CLI)
DEFAULT_MODEL=gemini/gemini-1.5-flash
DEFAULT_WORD_COUNT=100
DEFAULT_PROMPT=Describe this image in detail, including what you see, the setting, colors, composition, and any notable features or elements.
MAX_CONCURRENCY=5
MAX_FILE_SIZE_MB=10
MAX_AUDIO_SIZE_MB=100
RETRY_ATTEMPTS=3
TIMEOUT_SECONDS=30

# Testing Notes:
# - At least one API key is required for integration tests
# - Image analysis tests need: OPENAI_API_KEY, AZURE_OPENAI_KEY, GEMINI_API_KEY, or ANTHROPIC_API_KEY
# - Audio transcription tests need: OPENAI_API_KEY or AZURE_OPENAI_KEY
# - Tests will FAIL IMMEDIATELY if required API keys are missing (fail-fast philosophy)